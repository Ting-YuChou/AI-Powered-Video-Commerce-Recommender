{$CADDY_SITE_ADDRESS::80} {
    encode zstd gzip

    route {
        @gateway path /api/* /health /livez /readyz /docs /redoc /openapi.json
        reverse_proxy @gateway gateway-api:8000

        root * /srv
        try_files {path} /index.html
        file_server
    }
}
