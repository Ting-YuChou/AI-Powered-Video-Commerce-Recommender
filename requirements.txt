# Unified dependency entrypoint.
#
# Production/runtime deployments should install `requirements.runtime.txt`.
# Local development and CI can install this file to get runtime + test tooling.

-r requirements.runtime.txt
-r requirements-dev.txt
